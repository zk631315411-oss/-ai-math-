# 05 — 教材系统

教材系统负责 PDF 渲染、页码到章节的映射、页面标记和教材偏好持久化。

## 页码映射

核心设计：用确定性的页码→章节映射替代向量检索。

```
PDF 书签 (PyMuPDF 提取)
  +
MD 正文标题 (正则解析 34 个节级标题)
  │
  └─ 按章节号匹配 → textbook_sections 表
     │
     └─ sequence_id (五级 ID) + content (MD 全文) + page_range
```

每次提问时，`get_page_context(textbook_id, page)` 直接查 SQLite 获取当前页对应的章节 MD 原文，不再依赖向量相似度搜索。

## 教材渲染

[PDFViewer.tsx](../frontend/src/components/PDFViewer.tsx) — 核心组件：

- **PDF 渲染**：使用 `react-pdf` 逐页渲染
- **翻页**：支持滚动翻页、页码跳转、目录导航
- **适应平板**：竖屏模式全屏 PDF + 浮动 AiBall 提问球
- **双页视图**：桌面端可选双页并排

## 页面标记系统

[PageMarker.tsx](../frontend/src/components/PageMarker.tsx) — 学生可以在教材上做标记：

- **标记类型**：疑问（?）、重点（★）、已掌握（✓）
- **颜色编码**：黄=疑问、蓝=重点、绿=掌握
- **持久化**：`textbook_progress` 表存储标记位置与类型
- **悬停弹窗**：[MarkerPopover.tsx](../frontend/src/components/MarkerPopover.tsx) 显示标记详情，KaTeX 渲染公式

## 教材章节结构

`textbook_sections` 表存储了 35 条章节记录（34 节 + 1 答案区）：

```sql
CREATE TABLE textbook_sections (
    id TEXT,
    textbook_id TEXT,      -- "高代上-丘维声"
    sequence_id TEXT,      -- "V1-C01-S03"
    chapter_name TEXT,     -- "行列式的展开"
    content TEXT,          -- MD 原文全文
    start_page INTEGER,    -- 章节起始页
    end_page INTEGER       -- 章节结束页
);
```

## 教材选择

[TextbookViewer.tsx](../frontend/src/components/TextbookViewer.tsx) — 教材 MD 源码查看 + 教材选择。

系统当前内置：《高等代数（上册）》丘维声。

## 关键文件

| 文件 | 职能 |
|------|------|
| [textbook.py](../app/routers/textbook.py) | 教材查询 API |
| [textbook_section_db.py](../app/db/textbook_section_db.py) | 章节页映射查询 |
| [textbook_db.py](../app/db/textbook_db.py) | 教材元数据 |
| [PDFViewer.tsx](../frontend/src/components/PDFViewer.tsx) | PDF 渲染 |
| [TextbookViewer.tsx](../frontend/src/components/TextbookViewer.tsx) | 教材选择与查看 |
| [PageMarker.tsx](../frontend/src/components/PageMarker.tsx) | 页面标记 |
| [MarkerPopover.tsx](../frontend/src/components/MarkerPopover.tsx) | 标记弹窗 |
