# 本地部署环境清单

> 适用项目：智学助手（学数有道）  
> GitHub：https://github.com/zk631315411-oss/-ai-math-

---

## 一、基础环境

| 项目 | 最低版本 | 检查命令 |
|------|---------|---------|
| Python | >= 3.10 | `python --version` |
| Node.js | >= 18 | `node --version` |
| npm | >= 9 | `npm --version` |
| Git | 任意 | `git --version` |

---

## 二、Python 依赖

```bash
pip install -r requirements.txt
```

核心包：fastapi、uvicorn、openai、neo4j、dashscope、passlib、python-jose、sse-starlette

（全部用 pip 安装，无需 conda）

---

## 三、前端依赖

```bash
cd frontend
npm install
```

---

## 四、外部服务

### 4.1 Neo4j 图数据库（必需）

下载 Neo4j Desktop：https://neo4j.com/download/

1. 安装后创建本地 DBMS，默认连接 `bolt://localhost:7687`
2. 设置用户名/密码（记下来，填到 .env 里）
3. 知识图谱数据需单独导入（见第七节）

### 4.2 LLM API Key（至少一个）

| 用途 | 推荐 | 获取地址 |
|------|------|---------|
| 主问答 | 阿里 DashScope（通义千问） | https://dashscope.aliyun.com |
| 备选 | DeepSeek | https://platform.deepseek.com |

免费额度足够本地测试。

---

## 五、环境变量配置

将项目根目录的 `.env.example` 复制为 `.env`：

```bash
cp .env.example .env
```

然后编辑 `.env`，**必填**以下字段：

```ini
# JWT 密钥（随便写一串复杂字符）
JWT_SECRET=随便写一长串比如_zkmath2024secret

# LLM API（二选一）
LLM_API_KEY=你的dashscope或deepseek的key
LLM_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL=qwen-plus

# Neo4j
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=你设的neo4j密码
```

---

## 六、启动

```bash
# 终端1：后端
uvicorn app.main:app --host 0.0.0.0 --port 8000

# 终端2：前端
cd frontend && npm run dev
```

或者直接双击 `start.bat`（Windows）。

浏览器打开 `http://localhost:5173`。

---

## 七、教材数据

项目已内置 `data/learning.db`（含 3 本教材），克隆后教材数据开箱即用，无需额外导入。


