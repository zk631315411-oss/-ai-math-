# 项目交接提示

## 环境

| | 本地 | 云端 |
|--|------|------|
| 地址 | localhost:5173 :8000 | http://8.134.195.113 |
| 代码 | `d:\ai-math` | `/opt/ai-math`（git clone） |
| 数据库 | `d:\ai-math\data\learning.db`（SQLite） | `/opt/ai-math/data/learning.db`（独立副本） |
| Neo4j | 同一个 Aura 实例 `c60acc31.databases.neo4j.io` | 同左 |
| LLM API | 同阿里云 DashScope 账号 | 同左 |
| 服务管理 | 手动 `uvicorn` / `npm run dev` | systemd `ai-math.service` |

**关键**：本地和云端的 SQLite 数据库是**两套独立数据**，互不同步。Neo4j 和 LLM API 共享同一实例。

## 常用操作

### 本地改代码 → 上云

```bash
git add . && git commit -m "xxx" && git push origin main
```
然后 SSH 到云：
```bash
ssh root@8.134.195.113   # 密码 Zhangkai@2004
cd /opt/ai-math && git pull && systemctl restart ai-math
```

前端构建后需上传 dist：
```bash
cd frontend && npm run build
# 用 paramiko/sftp 上传 frontend/dist/ 到 /opt/ai-math/frontend/dist/
```

### 查看云服务状态

```bash
ssh root@8.134.195.113 "systemctl status ai-math"
```

### 查看云端日志

```bash
ssh root@8.134.195.113 "journalctl -u ai-math -n 50 --no-pager"
```

### 重启云服务

```bash
ssh root@8.134.195.113 "systemctl restart ai-math"
```

### 直接操作云数据库

```bash
ssh root@8.134.195.113 "cd /opt/ai-math && venv/bin/python -c \"...\""
# 或
ssh root@8.134.195.113 "sqlite3 /opt/ai-math/data/learning.db \"SELECT ...\""
```

### Neo4j 不可用时

登录 https://console.neo4j.io 检查 Aura 实例是否运行。免费实例闲置一段时间会自动暂停，手动恢复后等 1-2 分钟即可。

## 项目结构速查

```
ai-math/
├── app/routers/     # API 路由
├── app/services/    # 业务逻辑（10 个模块）
├── app/db/          # 数据访问（16 个模块）
├── frontend/src/    # React 前端
├── pipeline/        # Neo4j 图谱构建（离线）
├── tests/           # 测试
├── 项目详细说明/     # 架构文档（9 个 md）
└── 参考文库/         # 教育理论文献
```

## 关键外部依赖

| 依赖 | 网址 | 说明 |
|------|------|------|
| GitHub | github.com/zk631315411-oss/-ai-math- | 代码仓库 |
| Neo4j Aura | console.neo4j.io | 知识图谱，免费实例注意暂停 |
| 阿里云 ECS | 8.134.195.113 | 云服务器 |
| 阿里云 DashScope | dashscope.aliyun.com | LLM API（问答 + 诊断 + 截图） |

## 架构文档入口

- 项目总览：[项目总体介绍.md](项目总体介绍.md)
- 板块详解：[项目详细说明/00-总览.md](项目详细说明/00-总览.md)
- 画像数据流：已合并到项目详细说明/02-学生画像.md
