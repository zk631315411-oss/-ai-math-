# 认知诊断 V2 后续事项

## 上线前

- 在 `shadow` 模式采集首批真实证据，人工抽查引用、行为分类和 KG 命中。
- 为四个评分器建立固定合成回归集和少量教师标注集。
- 监控评分成功率、拒绝率、KG 空候选率、引用失败率和重复消费率。
- 确认 `shadow` 数据质量后依次开启 `stage_only` 和 `full`。

## 后续迭代

- 为教师复核增加 `review_status` 和 `human_label`。
- 引入可靠的章节完成事件后评估是否替代固定五事件窗口。
- 有足够标注后校准 Stage 规则、证据强度和15维聚合阈值。
- 单独设计即时认知卡点与 QA 的解耦协议；不与长期画像评分重新混合。
