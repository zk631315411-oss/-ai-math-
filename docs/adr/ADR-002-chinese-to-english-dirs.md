# ADR-002: 目录结构从中文改为英文

**日期**：2026-07-03

## 背景

`qa回答模块/`、`认知诊断模块/` 等中文目录名散落在项目根目录，不符合 Python 包命名规范。

## 决策

中文目录迁入 `app/services/` 下，改为英文名（`qa/`、`diagnosis/`）。

## 理由

Python 包名应为全小写英文；中文目录破坏 import 可读性；业务逻辑应归入 app/ 而非散落根目录。

## 后果

- 29 个文件的 import 路径需要更新
- `app/modules/` 桥接层删除（16 文件零引用）
