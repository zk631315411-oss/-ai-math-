# Practice V2 Deferred TODO

These items are deliberately outside the current textbook-first rollout:

- Trusted textbook pool is insufficient: design AI gap-question authoring.
- Add deterministic parameter variants for textbook items.
- Isolate author and independent reviewer model contexts.
- Add generated-item schema, math verification, duplicate detection and student visibility rules.
- Add trusted-item fallback when generated-question work fails.

The current runtime does not call the AI authoring path. This file mirrors the
deferred items from the competition product backlog until that backlog is
rewritten in a stable encoding.
